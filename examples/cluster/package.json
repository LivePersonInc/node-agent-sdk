{
  "name": "hashring-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "scripts": {
    "start": "docker-compose up -d && docker-compose logs -f app",
    "stop": "docker-compose kill && docker-compose rm -f"
  },
  "dependencies": {
    "task-sharding": "^0.0.2"
  }
}
